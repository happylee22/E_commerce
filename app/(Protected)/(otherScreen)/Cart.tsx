import { Cart } from '@/components/Cart';
import Wrapper from '@/components/Ui/Wrapper';
import React from 'react';

const cart = () => {
  return (
    <Wrapper>
      <Cart />
    </Wrapper>
  );
};

export default cart;
